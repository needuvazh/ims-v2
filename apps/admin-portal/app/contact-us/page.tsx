import { buildPublicMetadata } from '../_components/public-metadata';

export { default } from '../contact/page';

export const metadata = buildPublicMetadata({
  title: 'Contact Al-Saud Training Institute',
  description:
    'Contact Al-Saud Training Institute in Muscat for course dates, pricing, group training, and admissions support.',
  path: '/contact-us',
});
